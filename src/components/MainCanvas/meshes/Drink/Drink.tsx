import { FC } from "react";
import { DrinkProps } from "./Drink.types";

export const Drink: FC<DrinkProps> = ({
  modelPath,
  position,
  rotation
}) => {
  return (
    <mesh>
      
    </mesh>
  );
};
